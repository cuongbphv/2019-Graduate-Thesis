const sidebar = state => state.sidebar
const device = state => state.device
const language = state => state.language
const size = state => state.size

export default {
  sidebar,
  device,
  language,
  size
}
